# ------------------------------------------------------------------
#  APP
# ------------------------------------------------------------------
# The URL of your app, required for authentication.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# For NextAuth v5 in production, set ONE of the following to your public base URL:
# Prefer AUTH_URL. NEXTAUTH_URL is also supported for backwards-compat.
# Example: https://yourdomain.com
# On Netlify/Vercel, previews have dynamic hosts; see AUTH_TRUST_HOST below.
# AUTH_URL="https://your-production-domain.com"
# NEXTAUTH_URL="https://your-production-domain.com"

# When deploying behind a proxy/CDN (Netlify, Vercel, Cloudflare, nginx, etc.),
# enabling this allows NextAuth to trust the forwarded host header so callback
# URLs are built with the correct domain at runtime (including previews).
# Set to true in your hosting provider's environment settings.
# AUTH_TRUST_HOST=true

# ------------------------------------------------------------------
#  DATABASE
# ------------------------------------------------------------------
# Replace with your actual PostgreSQL connection string.
DATABASE_URL="postgresql://user:password@host:port/db"

# ------------------------------------------------------------------
#  AUTHENTICATION (NextAuth.js)
# ------------------------------------------------------------------
# A secret key for signing authentication tokens.
# You can generate a new one by running: openssl rand -hex 32
AUTH_SECRET="your-super-secret-auth-secret-generated-with-openssl"

# ------------------------------------------------------------------
# OAUTH (Google & GitHub)
# ------------------------------------------------------------------
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Provider Redirect URIs (register these in provider settings):
#   Google Authorized redirect URIs:
#     http://localhost:3000/api/auth/callback/google
#     https://YOUR_DOMAIN/api/auth/callback/google
#   GitHub Authorization callback URL(s):
#     http://localhost:3000/api/auth/callback/github
#     https://YOUR_DOMAIN/api/auth/callback/github
# If you use Netlify/Vercel previews, either add the preview URL(s) too or set
# AUTH_TRUST_HOST=true and use a single production OAuth app dedicated to the
# stable domain. Many providers do NOT support wildcards for redirect URIs.

# ------------------------------------------------------------------
#  EMAIL (Resend)
# ------------------------------------------------------------------
RESEND_API_KEY=
EMAIL_FROM="Ecommerce <no-reply@your-domain.com>"

# SMTP (use for local testing with MailHog)
# Set MAIL_PROVIDER=SMTP to route emails through SMTP instead of Resend
MAIL_PROVIDER=SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# ------------------------------------------------------------------
#  TWO-FACTOR AUTHENTICATION (TOTP)
# ------------------------------------------------------------------
# The issuer name displayed in authenticator apps (e.g., Google Authenticator).
# Optional in development for convenience; recommended to set in production.
TWO_FACTOR_ISSUER="AuthKit"